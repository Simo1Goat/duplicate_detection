#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <conio.h>
#include <ctype.h>

void ajout(char** ,char* );

char* extraire_min(char**);

char** LireToTas(char* );

char** MappingToTable(char* );
